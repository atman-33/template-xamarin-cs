<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Template.Views.MainPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="{Binding Title}">

    <ContentPage.Resources>
        <Thickness x:Key="pageButtonMargin">0,5,5,5</Thickness>
    </ContentPage.Resources>

    <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">

        <StackLayout Margin="{StaticResource pageButtonMargin}">
            <Label Text="Page1:画面遷移、ダイアログ、デバイス情報取得" />
            <Button Command="{Binding Page001ViewButton}" Text="Page1" />
        </StackLayout>

        <StackLayout Margin="{StaticResource pageButtonMargin}">
            <Label Text="Page2:カメラ起動" />
            <Button Command="{Binding Page002ViewButton}" Text="Page2" />
        </StackLayout>

        <StackLayout Margin="{StaticResource pageButtonMargin}">
            <Label Text="Page3:QRコード読み込み" />
            <Button Command="{Binding Page003ViewButton}" Text="Page3" />
        </StackLayout>

        <StackLayout Margin="{StaticResource pageButtonMargin}">
            <Label Text="Page4:入力内容をDB登録" />
            <Button Command="{Binding Page004ViewButton}" Text="Page4" />
        </StackLayout>

    </StackLayout>

</ContentPage>