<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Template.Views.Page001View"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:prism="http://prismlibrary.com"
    Title="{Binding Title}"
    prism:ViewModelLocator.AutowireViewModel="True">

    <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">

        <Label Text="Welcome to Xamarin Forms and Prism!" />

        <Label Text="{Binding LabelC}" />
        <Button Command="{Binding ButtonC}" Text="button c" />

        <Button Command="{Binding NextCommand}" Text="next page" />

        <Button Command="{Binding MessageCommand}" Text="message" />

    </StackLayout>

</ContentPage>